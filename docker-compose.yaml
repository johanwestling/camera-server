services:
  app:
    image: node
    command: sleep infinity
    working_dir: /var/www
    environment:
      HOSTNAME: 0.0.0.0
      PORT: 8080
      SERVER: http://localhost:8080
    ports:
      - 8080:8080
    volumes:
      - ./app:/var/www
